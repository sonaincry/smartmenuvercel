class Font {
  constructor(fontId, fontName, fontPath, isDeleted) {
    this.fontId = fontId;
    this.fontName = fontName;
    this.fontPath = fontPath;
    this.isDeleted = isDeleted;
  }
}

export default Font;
