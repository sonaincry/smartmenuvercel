class BoxItem {
  constructor(boxId, fontId, fontSize, textFormat, boxType, boxColor) {
    this.boxId = boxId;
    this.fontId = fontId;
    this.fontSize = fontSize;
    this.textFormat = textFormat;
    this.boxType = boxType;
    this.boxColor = boxColor;
  }
}

export default BoxItem;
